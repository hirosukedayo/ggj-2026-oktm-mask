# Unify Button Typography

- [x] Fix Prologue Button Font <!-- id: 1 -->
    - [x] Update `.button` in `app/page.module.css` to use `font-family: inherit`.
- [x] Verify inheritence <!-- id: 2 -->
    - [x] Ensure `input`, `textarea`, `select` also inherit if used later (preventative).
